{% extends 'MesdOrcaseAppBundle:Index:index.html.twig' %}

{% block main %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Actions</th>
                <th>Id</th>
                <th>Shorttitle</th>
                <th>Longtitle</th>
                <th>Body</th>
                <th>Bodyraw</th>
                <th>Usermodifiable</th>
                <th>Sticky</th>
                <th>Createdon</th>
                <th>Postedon</th>
                <th>Expiredon</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('notification_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('notification_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.shortTitle }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.longTitle }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.body }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.bodyRaw }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.userModifiable }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.sticky }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.createdOn }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.postedOn }}</a></td>
                <td><a href="{{ path('notification_show', { 'id': entity.id }) }}">{{ entity.expiredOn }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('notification_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock main %}
