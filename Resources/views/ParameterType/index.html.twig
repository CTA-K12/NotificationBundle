{% extends 'MesdOrcaseAppBundle:Index:index.html.twig' %}

{% block main %}

    <table class="table table-bordered table-condensed table-hover table-striped">
        <thead>
            <tr>
                <th>Actions</th>
                <th>Id</th>
                <th>Shortname</th>
                <th>Longname</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('parametertype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('parametertype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
                <td><a href="{{ path('parametertype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td><a href="{{ path('parametertype_show', { 'id': entity.id }) }}">{{ entity.shortName }}</a></td>
                <td><a href="{{ path('parametertype_show', { 'id': entity.id }) }}">{{ entity.longName }}</a></td>
                <td><a href="{{ path('parametertype_show', { 'id': entity.id }) }}">{{ entity.description }}</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('parametertype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock main %}
